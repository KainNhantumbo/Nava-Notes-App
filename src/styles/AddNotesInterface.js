import styled from "styled-components";
import { colors } from "./colors";

export const InterfaceStyles = styled.section`
  width: 100vw;
  height: 100vh;
  z-index: 500;
  background: rgba(${colors.backgroundColor}, .5);
`;